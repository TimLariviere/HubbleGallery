<?xml version="1.0" encoding="utf-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" xmlns:local="clr-namespace:HubbleGalleryFSharp" x:Class="HubbleGalleryFSharp.MainPage" Title="Hubble">
    <StackLayout>
        <Picker x:Name="FeedPicker"></Picker>
        <ListView x:Name="PicturesListView" RowHeight="120" VerticalOptions="FillAndExpand">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell>
                        <StackLayout Orientation="Horizontal">
                            <Image Source="{Binding ImageLink}" HeightRequest="120" WidthRequest="120" Aspect="AspectFill" />
                            <Label Text="{Binding Title}" VerticalOptions="CenterAndExpand" LineBreakMode="TailTruncation" />
                        </StackLayout>
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </StackLayout>
</ContentPage>